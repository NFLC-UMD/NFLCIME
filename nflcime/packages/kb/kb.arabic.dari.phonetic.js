NFLCIME.dispatchEvent( { type:'ModuleAdd', module:
{
	id:'kb.arabic.dari.phonetic',
	type:'keyboard layout',
	inheritance:['kb.arabic.farsi.phonetic']
	// exactly the same as the Farsi keyboard
}
} );